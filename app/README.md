# Task API

## Описание
REST API для управления задачами. Реализовано с использованием FastAPI.

## Возможности API
- Получение списка задач (`GET /tasks`)
- Создание новой задачи (`POST /tasks`)
- Обновление существующей задачи (`PUT /tasks/{id}`)
- Удаление задачи (`DELETE /tasks/{id}`)

## Установка и запуск

1. Клонировать репозиторий:
```bash
git clone https://github.com/your_username/task-api.git
cd task-api
